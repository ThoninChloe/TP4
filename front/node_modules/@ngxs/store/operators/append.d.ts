import { RepairType } from './utils';
/**
 * @param items - Specific items to append to the end of an array
 */
export declare function append<T>(items: T[]): (existing: RepairType<T>[]) => RepairType<T>[];
